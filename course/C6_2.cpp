#include <iostream>
#include <algorithm>
#include <vector>
/******
using namespace std;
bool compare(int a, int b);
int main()
{
    int i,n,k;

    cin>>n;
    cin>>k;
    vector<int> a(n);
    for(i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a.begin(),a.end(),compare);
    cout<<a[k-1]<<endl;
    return 0;
}
bool compare(int a,int b){
    return a<b;
}
*****/
